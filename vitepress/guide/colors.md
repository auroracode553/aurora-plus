<script setup>
import ColorTokenPalette from '../.vitepress/theme/components/ColorTokenPalette.vue';
</script>

# Color 颜色

Aurora Plus 使用全局语义颜色变量统一组件、业务内容与明暗主题的视觉表达。变量名称描述用途，而不是绑定某个组件，因此同一种状态可以在不同界面中保持一致。

<ColorTokenPalette />
