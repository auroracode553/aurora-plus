# Aurora UI 协作规则

## 文档职责边界

- 面向使用者的文档只负责 Aurora UI 本身：安装、引入、组件 API、服务 API、图标注册、主题变量、无障碍行为、迁移说明和可复制的 Vue 示例。
- `vitepress/index.md`、`vitepress/guide/**`、`vitepress/components/**` 以及 `ui/README.md` 均属于 UI 库文档。不得在这些文件中介绍文档站或组件库的构建、打包、调试、本地开发依赖、产物目录、启动、预览、部署、热更新、Markdown、Shiki、别名配置或其他工具链实现细节。
- 文档站的目录结构、依赖、脚本、示例加载机制和维护流程只能写在 `vitepress/README.md`。新增或调整文档站工具时，同步更新该 README，不要把说明带入 UI 使用指南。
- 组件文档源码中为实时示例保留的 `.vitepress/` 导入属于内部实现，不在面向用户的正文中解释，也不要将其作为 UI 库使用步骤。
- 文档页面为正常渲染所必需的 frontmatter、组件导入和 `?demo-source` 查询参数可以保留；它们属于构建语法，不应扩展为面向用户的工具链说明。
- 根目录 `README.md` 只保留仓库概览和 UI 库入口；不要复制文档站命令或运行说明。

## 修改与验证

- 修改文档时优先复用 `ui/README.md` 和组件源码中已确认的 API，避免写入未实现的属性、事件或依赖。
- 不要默认扫描或修改 `dist/`、`node_modules/`；不要在面向用户的 UI 文档中添加构建、调试或本地开发命令。文档站维护命令仅记录在 `vitepress/README.md`，并由使用者手动执行。
- 保持职责单一、按模块组织内容；单个源码文件有效代码行数不超过 3000 行。关键逻辑添加简明注释，示例中的数据和状态必须完整定义。

## 组件视觉与尺寸规则

- Aurora UI 默认沿用紧凑、克制的原有尺寸。未经明确要求，不得通过统一增加高度、宽度、内边距、间距或圆角来“优化”组件。
- 常用控件优先保证信息密度：默认按钮高 32px、小号 28px、大号 40px；按钮组默认控制尺寸 28px，浮动按钮组间距和内边距均为 2px。
- `connected` 按钮组采用 Apple 风格分段工具条：统一半透明材质容器、透明未选项和独立选中材质层；禁止恢复为硬边框拼接样式，选中状态优先使用 `aria-pressed="true"` 表达。
- 玻璃材质只通过透明度、边框、模糊、饱和度和轻量阴影表达，不得用厚重阴影、夸张圆角或额外留白制造体积感。默认应保持柔和，清透和 solid 作为可选材质。
- 整个 UI 库（包括按钮、按钮组、开关、复选框、下拉菜单、卡片、弹窗、菜单、工具栏、Tooltip、消息和列表）统一禁止高光渐变、内发光、反射层和厚重阴影；只保留扁平表面、细边框与轻微状态色变化。
- Message 等轻量浮层的轮廓优先使用分层材质阴影表达，不使用明显硬边框；阴影必须低对比、主题自适应，并保留键盘焦点轮廓。
- 列表、滚动容器等内容承载组件默认不得添加着色背景层；使用透明背景继承页面默认背景，只有业务明确要求时才允许通过局部变量覆盖。
- 文档 Demo 的预览容器也必须继承文档页面默认背景，不得在 `.au-doc-demo__preview` 或具体示例容器上重复添加 `--au-color-bg`、`--au-color-bg-soft` 等着色层。
- 单个组件需要放大时必须限定在明确的 size/variant 或局部 CSS 变量中，不得影响其他组件；优先提供紧凑默认值和可覆盖变量。
- 交互反馈应轻量且可中断，保留键盘焦点、`prefers-reduced-motion`、`prefers-reduced-transparency` 和高对比度回退。
